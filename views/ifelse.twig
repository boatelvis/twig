<ul>
    {% for word in words %}

        {% if word is null %}
        <p>Null element </p>
        {% elseif word | trim is empty %}
        <p>Empty element</p>
        {% else %}
        <li>{{ word }}</li>
        {% endif %}

    {% endfor %}
</ul>